// Importar todas as ações necessárias do Auth.js
//app\api\auth\[...nextauth]\route.ts
import { GET, POST } from "@/auth";

// Exportar as ações para que o Next.js possa usá-las
export { GET, POST };
