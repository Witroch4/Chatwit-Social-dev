// app/layout.tsx

import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { ThemeProvider } from "@/components/providers/theme-provider";
import { cn } from "@/lib/utils";
import { SessionProvider } from "next-auth/react";
import { SidebarProvider } from "@/components/ui/sidebar"; // Mantém SidebarProvider no RootLayout
import Navbar from "@/components/navbarGeral";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="pt-BR">
      <body className={cn(inter.className, "min-h-screen")}>
        <SessionProvider>
          <ThemeProvider
            attribute="class"
            defaultTheme="system"
            enableSystem
            disableTransitionOnChange
          >
             {/* Envolve toda a aplicação com SidebarProvider */}
              <div className="flex h-full min-h-screen">
                <Navbar /> {/* Navbar pode acessar o contexto do Sidebar */}
                <main className="flex-1 p-4 md:p-8">{children}</main>
              </div>

          </ThemeProvider>
        </SessionProvider>
      </body>
    </html>
  );
}
